<h1>Users</h1>

<!--ユーザー一覧-->
<% @books.each do |book| %>
<div>
  <p>image <%= image_tag book.user.get_profile_image(80,80) %></p>
  <p>name <%= book.title %></p>
 
  <td><%= link_to "Show", book_path(book.id) %></td>
  
</div>
<% end %>